.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
.env
.env.*
!.env.example

# Réglages Claude Code locaux (permissions spécifiques à la machine)
.claude/settings.local.json
data/sources/
data/cache/
data/irminsul.db
data/irminsul.db-*
tools/bin/
*.log

# Espace de travail Phase 0 (logs complets, mesures, index) — transitoire, régénérable
.irminsul/

# Données personnelles du joueur (UID, profil, exports compte) — ne jamais committer
profiles/player.yaml
*-GOOD.json
*_GOOD_*.json
*.good.json
good-export*.json

# Compte joueur importé (brut, normalisé, snapshots, rapports, recommandations).
# Tout data/account/ contient des données personnelles dérivées → jamais sur GitHub.
data/account/

# Monorepo web (apps/web, packages/*) — Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.turbo/

# Duo local runtime (sessions, journaux, checkpoints, rapports temporaires)
.duo/
# Outillage Codex/Duo local (abandonné — voir docs/reviews/CODEX_DUO_ACCESS_REPORT.md) :
# dossiers générés par les apps, config locale, verrous locaux. Jamais versionnés.
.agents/
.codex/
duo.config.json
docs/skills/*.local.json
scripts/*codex-login*
scripts/repair-resume-duo-script.ps1
*.backup-*

# Ressources desktop generees (prepare-desktop-web.mjs)
app/src-tauri/web/
app/src-tauri/web-template.db
src/irminsul/_buildinfo.py

# Memoire de l assistant : elle decrit la box et le compte du joueur.
# Elle vit desormais dans le depot PRIVE NylenIA/irminsul-prive (dossier memoire/).
.claude/agent-memory/
