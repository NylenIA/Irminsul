# Avancement Irminsul — recalculé (reproductible)

> Généré par `scripts/project_progress.py` depuis `PROJECT_PROGRESS.json` le 2026-07-22. Branche `feat/irminsul-complete-redesign`. Ne pas éditer à la main.

## Scores
- **MVP utilisable : 90%**
- **Vision complète : 90%**
- Confiance globale : moyenne (cœur moteur élevé ; surfaces web/UI récentes).
- Échelle : 0=absent, 25=squelette/mock/TODO, 50=happy-path peu validé, 75=intégré+testé cas majeurs, 100=prêt à livrer

## Détail par domaine
| Domaine | Poids | PoidsMVP | Score | Confiance | Preuve | Blocages |
|---|--:|--:|--:|---|---|---|
| Moteur de données et calcul | 25 | 30 | 91 | high | Moteur stats finales INTEGRE ; pytest 239 ; goldens vs jeu ; AUDIT SOURCE des formules (docs/engine/FORMULA_SOURCES.md, chaque constante sourcee KQM+corroboree) qui a corrige un VRAI bug DPS : coefficients transformatifs pre-5.2 -> post-5.2 (EC 2.0/OL 2.75/SC 1.5/Shatter 3.0, 3 sources concordantes, garde anti-regression) ; lunaires LC 1.8 / LCrys 1.6 (base LC corroboree Icy Veins) + EM lunaire partage 6EM/(EM+2000) + additives ; parite TS<->Python goldens croises ; Lunar-Bloom TRAITE PAR SOURCAGE (KQM : pas de multiplicateur de reaction propre -> famille 'directe' de capacite) : le moteur renvoie un message SOURCE pointant vers calculate_direct_hit au lieu d'un 'inconnu', parite py+ts testee | Lunar-Bloom RESOLU (sourcage KQM : aucun multiplicateur de reaction par conception ; base 'directe' par capacite a transcrire seulement si on cable un calcul par capacite) ; veille patch pour revalidation continue des constantes |
| Intégration moteur | 15 | 12 | 91 | high | Stats finales LIVE + apercu + reactions ; parite prouvee ; ADDITIVES dans l'UI (flatBaseDamage, aussi forwarde au sidecar) + lunaires LC/LCrys ; SIDECAR PYTHON PAR DEFAUT dans l'apercu (opt-out IRMINSUL_ENGINE=local, repli TS documente, provenance honnete, preuve E2E badge moteur python-sidecar obligatoire) | latence spawn-par-appel du sidecar (acceptable apercu, a pooler si generalise) |
| Application web | 15 | 10 | 89 | high | Next.js 16.2.9 ; 10 surfaces (+ /simulation gcsim) ; E2E 62/62 (workflows reels team-lab/simulation/rotations/compare/recommandations dont UI conditionnelle improve_current_team, export gcsim, marque) ; axe 0 critique partout ; resolution moteur unifiee (sidecar par defaut) | quelques micro-surfaces sans test unitaire composant (couvert E2E) |
| Application desktop | 10 | 12 | 85 | high | PROUVE bout-en-bout et REVALIDE (2026-07-20) avec le moteur a jour : rebuild sidecar (provenance commit=48f475a verifiee DANS l'app installee) + desktop:build + reinstall NSIS silencieuse + smoke installe TOUT VERT (7 routes 200, proxy.ts nonce POST/boot->403 x3, node lie au PID Tauri, zero orphelin x2, persistance DB, diagnostic sanitize) + cas d'erreur 4/4 chemin Unicode. L'app installee embarque lunaires+additives+sidecar-par-defaut. | MSI 1603 (per-machine sans elevation) ; non signe (SmartScreen) ; bind-race residuel accepte ; migration-on-update de la base %APPDATA% : desormais DETECTEE (Diagnostic) ET APPLIQUEE au boot (instrumentation.ts, runner idempotent/transactionnel, no-op aujourd'hui) -> reste a revalider par un desktop:build + smoke lors du prochain patch de schema (chemin de code identique a dev/E2E) |
| Team builder et simulation | 15 | 15 | 95 | medium | Team Lab + reactions + rotations + comparateur + recommandations ; /simulation : vraies sims gcsim + squelette depuis equipe enrichi du scan GOOD (perso/arme/sets + stats artefacts reelles sans double-comptage) ; EXPORT rotation Team Lab -> actions gcsim (rotationToGcsimActions, verbes+groupage, ordre seulement honnete). Chaine complete equipe->config gcsim quasi-prete. 113 vitest | energie/cooldown fin ; buffs par action (modele analytique -> gcsim comble le gros du besoin) |
| Persistance | 5 | 8 | 92 | high | Prisma 6 + SQLite ; TeamRepository CRUD + importTeams TRANSACTIONNEL ; export/import versionne irminsul-export/1.0 avec checksum ; couverture data-access 22 tests dont rollback transactionnel + fail-fast + remplace-par-nom + dedoublonnage ; chemin DB desktop = %APPDATA% CONFIRME (app_data_dir Tauri -> ensure_user_db, template pre-migre copie au 1er lancement JAMAIS ecrase, cargo lib 11/11) ; DETECTION de derive : checkSchemaDrift (3 etats, 5 tests) surfacee au Diagnostic (avertissement + re-import, prouve E2E) ; APPLICATION AUTO des migrations en attente : applyPendingMigrations (runner Node IDEMPOTENT + TRANSACTIONNEL tout-ou-rien, ne touche QUE les bases gerees Prisma, 4 tests dont PRESERVATION des donnees existantes + ROLLBACK sur SQL invalide) cable au boot via Next instrumentation.ts (nodejs only, non bloquant) ; SQL embarque via MIGRATIONS_BUNDLE genere de prisma/migrations (garde synchro noms+SQL) -> survit au bundle desktop ; no-op sur base a jour, register() au boot prouve par E2E 62/62 | runner d'auto-migration prouve en dev (next dev/E2E via le meme hook register()) + unit (application/no-op/base-non-geree/preservation/rollback) ; un desktop:build + smoke de MAJ (base ancienne -> migration appliquee au 1er boot) reste a executer au prochain build desktop pour revalidation bout-en-bout (chemin de code identique) |
| Design system et UX | 5 | 5 | 86 | medium | tokens OKLCH + primitives (Button/Card/Input/Select/ConfirmDialog/Toast/States/CharacterPicker) axe 0 critique ; BRAND_GUIDE.md ; LOGO Irminsul livre (SVG constellation-arbre degrade tokens, favicon /icon.svg, header logo+wordmark, animation legere reduced-motion safe, E2E) | Tooltip/Panel a la demande ; Tabs differe YAGNI |
| Tests et qualite | 5 | 4 | 88 | high | pytest 235 (ruff cable) ; vitest 160 (3 workspaces dans verify : data-access 22 dont rollback transactionnel + detection de derive + runner d'auto-migration, engine-client 113, ui 25) ; TOUTES les primitives testees unitairement en jsdom via Testing Library (Button/Input/Select/States/Card/ConfirmDialog + Toaster : mapping variante->classe, fusion className, passe-plat props natives, roles a11y status/alert/aria-live, cablage onConfirm/onCancel, ouverture modale reelle avec polyfill dialog.showModal/close honnete, liaison store<->React du Toaster : emission/pile/fermeture individuelle) ; E2E 62 (workflows reels + axe + moteur sidecar + toast) ; goldens croises TS<->Python ; garde manifest proxy ; ESLINT cable dans verify (typescript-eslint recommended, flat config) | regles Next-specifiques eslint-config-next (extension future) ; surfaces/pages Next (app-router) non testees unitairement (couvert E2E) ; scripts .mjs hors lint |
| Packaging et deploiement | 3 | 2 | 85 | high | NSIS installe/desinstalle/reinstalle PROUVE (per-user, sans admin) + icone de marque (icon.ico depuis le logo, desktop:build vert) ; signature de code ABANDONNEE par decision (distribution privee entre amis) -> plus un blocage ; chaine desktop complete pour l'usage vise | MSI requiert admin (par conception, non prioritaire) ; pas de signature = SmartScreen sur machines tierces (accepte) |
| Documentation et continuite | 2 | 2 | 84 | high | audit complet, DECISIONS, ARCHITECTURE, SESSION_HANDOFF ; FORMULA_SOURCES.md (chaque constante sourcee+verifiee) ; PATCH_WATCH.md (process de veille tracable, checklist par patch) ; BRAND_GUIDE.md + DESIGN_SYSTEM.md a jour ; README racine VERIFIE a jour (surfaces /simulation, additives, lunaires, sidecar, export gcsim, NSIS) + section 'Continuite de session' documentant le hook avec la commande correcte | hook SessionStart non enregistre : la commande d'origine (pwsh) est INVALIDE ici (pwsh absent -> exit 127), corrigee en 'powershell -NoProfile -ExecutionPolicy Bypass -File ...' et documentee ; l'edition de .claude/settings.json pour un hook auto-execute est bloquee par le classifier en mode auto -> registration = action utilisateur |

_Recalcule : `python scripts/project_progress.py`._
