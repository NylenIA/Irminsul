name: irminsul
description: "Irminsul — compagnon de theorycrafting Genshin Impact (local-first)."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  file_picker: ^8.1.2
  flutter_svg: ^2.0.10+1
  intl: any

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
