{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions minimales : fenêtre principale + ouverture de fichier (import GOOD).",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
