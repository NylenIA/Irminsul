{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions minimales : fenêtre principale uniquement (core).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
